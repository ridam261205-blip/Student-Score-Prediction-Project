# Student-Score-Prediction-Project
A machine learning project that predicts student scores using Python with NumPy, Pandas, and Matplotlib. Includes data preprocessing with LabelEncoder, StandardScaler, and MinMaxScaler, model evaluation using MAE, MSE, and RMSE, and visual analysis through bar charts and scatter plots.
